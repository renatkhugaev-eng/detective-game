# 🔍 Детектив - Тайны ждут разгадки

Детективная игра с изометрическим видом, созданная на Three.js.

## 🎮 Особенности

- **Изометрическая камера** - классический вид для детективных игр
- **Мультяшная графика** - стилизованные персонажи и окружение
- **Система улик** - собирайте улики и раскрывайте тайны
- **Диалоги с NPC** - опрашивайте свидетелей
- **Десктоп приложение** - работает как отдельная программа через Electron

## 🚀 Быстрый старт

### Установка зависимостей

```bash
npm install
```

### Режим разработки (браузер)

```bash
npm run dev
```

Откройте http://localhost:5173 в браузере.

### Режим разработки (Electron)

```bash
npm run electron:dev
```

### Сборка для продакшена

```bash
# Сборка веб-версии
npm run build

# Сборка десктоп приложения (Windows)
npm run electron:build:win

# Сборка десктоп приложения (macOS)
npm run electron:build:mac

# Сборка десктоп приложения (Linux)
npm run electron:build:linux
```

## 🎯 Управление

| Клавиша | Действие |
|---------|----------|
| **WASD / Стрелки** | Движение персонажа |
| **Клик мышью** | Перемещение к точке |
| **E** | Взаимодействие с объектом |
| **Tab** | Открыть инвентарь улик |
| **Esc** | Пауза |

## 📁 Структура проекта

```
src/
├── core/           # Ядро игры
│   ├── Game.ts         # Главный класс
│   ├── Renderer.ts     # Рендерер с пост-обработкой
│   ├── Loop.ts         # Игровой цикл
│   ├── SceneManager.ts # Менеджер сцен
│   └── IsometricCamera.ts # Изометрическая камера
├── entities/       # Игровые сущности
│   ├── Player.ts       # Игрок (детектив)
│   └── InteractiveObject.ts # Интерактивные объекты
├── systems/        # Системы
│   ├── InputSystem.ts  # Система ввода
│   ├── AudioSystem.ts  # Аудио система
│   └── GameStore.ts    # Хранилище состояния
├── scenes/         # Сцены
│   ├── BaseScene.ts    # Базовый класс сцены
│   ├── MenuScene.ts    # Главное меню
│   ├── GameScene.ts    # Игровая сцена
│   └── LoadingScene.ts # Загрузка
├── utils/          # Утилиты
│   └── AssetLoader.ts  # Загрузчик ресурсов
├── types/          # TypeScript типы
│   └── index.ts
├── assets/         # Ресурсы
│   ├── models/     # 3D модели
│   ├── textures/   # Текстуры
│   ├── sounds/     # Звуки
│   └── fonts/      # Шрифты
├── main.ts         # Точка входа
└── style.css       # Стили
```

## 🛠 Технологии

- **Three.js** - 3D графика
- **TypeScript** - типизация
- **Vite** - сборка
- **Electron** - десктоп приложение
- **Zustand** - управление состоянием
- **Howler.js** - аудио
- **Postprocessing** - визуальные эффекты

## 🎨 Добавление контента

### Новая улика

```typescript
const newClue = new InteractiveObject({
  id: 'clue_unique_id',
  type: 'clue',
  name: 'Название улики',
  description: 'Описание улики',
  position: new THREE.Vector3(x, y, z),
  onInteract: () => {
    GameStore.getState().addFoundClue('clue_unique_id');
    this.showDialogue('Текст при нахождении улики');
  }
});
```

### Новый NPC

```typescript
const newNPC = new InteractiveObject({
  id: 'npc_unique_id',
  type: 'npc',
  name: 'Имя персонажа',
  description: 'Описание',
  position: new THREE.Vector3(x, y, z),
  onInteract: () => {
    this.showDialogue('Диалог персонажа');
  }
});
```

## 📝 TODO

- [ ] Добавить больше уровней/локаций
- [ ] Система диалогов с выбором ответов
- [ ] Сохранение/загрузка игры
- [ ] Звуковое сопровождение
- [ ] Анимации персонажей
- [ ] Система подсказок
- [ ] Финальная развязка дела

## 📄 Лицензия

MIT License
